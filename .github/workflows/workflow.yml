on:
    push:
    workflow_dispatch:

env:
    NSUnbufferedIO: YES

# defaults:
#     run:
#         shell: bash

jobs:
    job:
        runs-on: macos-26
        steps:
            - run: |
                which -a cmake || true
                which -a gmake || true
                which -a gnumake || true
                which -a make || true
