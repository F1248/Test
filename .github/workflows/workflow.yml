on:
    push:
    workflow_dispatch:

jobs:
    job:
        runs-on: macos-15
        steps:
    
            - run: pwd
            - uses: actions/checkout@main
            - run: |
                pwd
                ls
