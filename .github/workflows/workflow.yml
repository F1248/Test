on:
  push:
  workflow_dispatch:
    inputs:
      Test:
        type: string

jobs:
  job:
    runs-on: macos-latest
    steps:

      - run: |
          echo $GITHUB_WORKSPACE
          pwd
          ls
