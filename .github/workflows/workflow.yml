on:
  push:
  workflow_dispatch:

jobs:
  job:
    runs-on: macos-15
    steps:

      - run: |
          mkdir Test.app/Directory
          echo "Test" > Test.app/Directory/File.txt

      - name: Upload Test
        uses: actions/upload-artifact@main
        with:
          name: Test
          path: Test.app
