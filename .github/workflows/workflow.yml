on:
    push:
    workflow_dispatch:

jobs:
    job:
        runs-on: macos-15
        steps:
            - name: Setup Xcode
              uses: maxim-lobanov/setup-xcode@master
              with:
                  xcode-version: latest
