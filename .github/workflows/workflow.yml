on:
    push:
    workflow_dispatch:

env:
    NSUnbufferedIO: YES

# defaults:
#     run:
#         shell: bash

jobs:
    job-1:
        runs-on: macos-26
        steps:
            - run: |
                echo test > test.txt
                sed -i "s|e|o|g" test.txt

    job-2:
        runs-on: macos-26
        steps:
            - run: |
                echo test > test.txt
                sed -i "" "s|e|o|g" test.txt
