on:
  - push
  - workflow_dispatch

jobs:
  job:
    runs-on: macos-latest
    steps:
      - run: |
          /usr/bin/sudo /usr/sbin/softwareupdate --install --all --include-config-data --restart --agree-to-license --verbose
