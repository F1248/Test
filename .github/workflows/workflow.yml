on:
  push:
  workflow_dispatch:

jobs:
  job:
    runs-on: macos-latest
    steps:

      - uses: actions/checkout@main
        with: 
          repository: F1248/Genius
          fetch-depth: 0

      - run: |
          echo $GITHUB_WORKSPACE
          pwd
          ls
