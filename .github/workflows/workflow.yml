on:
  - push
  - workflow_dispatch

jobs:
  job:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          /bin/ls
          /usr/bin/sudo ./test.exp
          /usr/bin/sudo /usr/sbin/softwareupdate --install --all --include-config-data --stdinpass "password" --restart --agree-to-license --verbose
