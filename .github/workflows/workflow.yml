on:
  push:
  workflow_dispatch:

jobs:
  job:
    runs-on: macos-15
    steps:

      - run: |
          PATH="/usr/bin:/bin"
          echo 42
          echo $PATH
          echo 43

      - run: |
          echo 44
          echo $PATH
          echo 45
          ps -p $$ 
