on:
    push:
    workflow_dispatch:

jobs:
    job:
        runs-on: macos-15
        steps:

            - name: Checkout
              uses: actions/checkout@main
              with:
                repository: sindresorhus/Pasteboard-Viewer
    
            - run: xcodebuild CODE_SIGNING_ALLOWED=NO -scheme "Pasteboard Viewer" -destination "generic/platform=macOS"
