on:
    push:
    workflow_dispatch:

env:
    NSUnbufferedIO: YES

defaults:
    run:
        shell: bash

jobs:
    job:
        runs-on: macos-26
        steps:
            - run: |
                echo "First step 1!"
                make test
                echo "First step 2!"
            - run: echo "Second step!"
