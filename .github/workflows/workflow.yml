on:
    push:
    workflow_dispatch:

# defaults:
#     run:
#         shell: bash

jobs:
    job:
        runs-on: macos-15
        steps:
            - run: |
                /usr/bin/fdesetup status
                echo $?
            - run: |
                /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
                echo $?
