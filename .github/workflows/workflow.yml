on:
    push:
    workflow_dispatch:

defaults:
    run:
        shell: bash

jobs:
    job:
        runs-on: macos-15
        steps:
            - run: sudo du -d 1 -h /
            - run: sudo du -d 2 -h /
            - run: sudo du -d 3 -h /
            - run: sudo du -d 4 -h /
