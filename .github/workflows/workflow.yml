on:
    push:
    workflow_dispatch:

jobs:
    job:
        runs-on: macos-13
        steps:
    
            - run: |
                system_profiler SPHardwareDataType
                sysctl hw.cpufrequency
