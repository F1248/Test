on:
  push:
  workflow_dispatch:

jobs:
  job:
    runs-on: macos-13
    steps:

      - run: |
          arch
          system_profiler SPSoftwareDataType
          system_profiler SPHardwareDataType
