on:
    push:
    workflow_dispatch:

jobs:
    job:
        runs-on: macos-15
        steps:
    
            - run: pwd
            - uses: actions/checkout@main
              with:
                fetch-depth: 0
            - run: |
                pwd
                ls
