on:
  push:
    paths:
      Test1.txt
      .github/workflows/workflow.yml
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          which mkdir
          which cp
          echo Test 42
