on:
    push:
    workflow_dispatch:

env:
    NSUnbufferedIO: YES

defaults:
    run:
        shell: bash

jobs:
    job:
        runs-on: macos-26
        steps:
            - uses: https://github.com/maxim-lobanov/setup-xcode@master
              with:
                xcode-version: "26.4.0"
